{
  "buildCommand": "pnpm --filter web build",
  "outputDirectory": ".next",
  "installCommand": "pnpm install --filter web --filter=@vault/ui --filter=@vault/types --filter=@vault/config --no-frozen-lockfile",
  "rootDirectory": "apps/web"
}
