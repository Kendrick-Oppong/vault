import type { VideoContainer, AudioFormat } from "@vault/types";

export const VIDEO_CONTAINERS: VideoContainer[] = ["mp4", "mkv"];

export const AUDIO_FORMATS: AudioFormat[] = ["mp3", "m4a", "opus", "flac", "wav"];

export const AUDIO_BITRATES = [320, 256, 192, 128, 96];
